import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-training-section-7',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
